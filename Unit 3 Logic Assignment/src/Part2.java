/*Name: Zihe Zhang
 * Class: ICS2O1-02
 * Date: 22/03/2017
 * Teacher: Mr Naccorato
 * Program summary: Program that asks users to input 10 people's weights and takes the input and uses it to determine if the people are light, medium or heavy weight.
 */

//Imports scanner from the library
import java.util.Scanner;

public class Part2 
{

	public static void main(String[] args) 
	{
		// TODO Auto-generated method stub
		//Declaration that variable holds the sub program
		Scanner reader = new Scanner (System.in);
		
		System.out.println("Please enter 10 people's weight");
		//reserves and reads the user's input
		int weight1 = reader.nextInt();
		
		//analyses user input determines what to do next if the input is less than 60, between 60 and 80 or over 80
		if (weight1<60)
			System.out.println("You are light weight");
		else if (weight1>=60 && weight1<80)
			System.out.println("You are medium weight");
		else 
			System.out.println("You are heavy weight");
		
		//reserves and reads the user's input
		int weight2 = reader.nextInt();
		
		//analyses user input determines what to do next if the input is less than 60, between 60 and 80 or over 80
		if (weight2<60)
			System.out.println("You are light weight");
		else if (weight2>=60 && weight2<80)
			System.out.println("You are medium weight");
		else 
			System.out.println("You are heavy weight");
		
		//reserves and reads the user's input
		int weight3 = reader.nextInt();
		
		//analyses user input determines what to do next if the input is less than 60, between 60 and 80 or over 80
		if (weight3<60)
			System.out.println("You are light weight");
		else if (weight3>=60 && weight3<80)
			System.out.println("You are medium weight");
		else 
			System.out.println("You are heavy weight");
		
		//reserves and reads the user's input
		int weight4 = reader.nextInt();
		
		//analyses user input determines what to do next if the input is less than 60, between 60 and 80 or over 80
		if (weight4<60)
			System.out.println("You are light weight");
		else if (weight4>=60 && weight4<80)
			System.out.println("You are medium weight");
		else 
			System.out.println("You are heavy weight");
		
		//reserves and reads the user's input
		int weight5 = reader.nextInt();

		//analyses user input determines what to do next if the input is less than 60, between 60 and 80 or over 80
		if (weight5<60)
			System.out.println("You are light weight");
		else if (weight5>=60 && weight5<80)
			System.out.println("You are medium weight");
		else 
			System.out.println("You are heavy weight");
		
		//reserves and reads the user's input
		int weight6 = reader.nextInt();

		//analyses user input determines what to do next if the input is less than 60, between 60 and 80 or over 80
		if (weight6<60)
			System.out.println("You are light weight");
		else if (weight6>=60 && weight6<80)
			System.out.println("You are medium weight");
		else 
			System.out.println("You are heavy weight");
		
		//reserves and reads the user's input
		int weight7 = reader.nextInt();

		//analyses user input determines what to do next if the input is less than 60, between 60 and 80 or over 80
		if (weight7<60)
			System.out.println("You are light weight");
		else if (weight7>=60 && weight7<80)
			System.out.println("You are medium weight");
		else 
			System.out.println("You are heavy weight");
		
		//reserves and reads the user's input
		int weight8 = reader.nextInt();

		//analyses user input determines what to do next if the input is less than 60, between 60 and 80 or over 80
		if (weight8<60)
			System.out.println("You are light weight");
		else if (weight8>=60 && weight8<80)
			System.out.println("You are medium weight");
		else 
			System.out.println("You are heavy weight");
		
		//reserves and reads the user's input
		int weight9 = reader.nextInt();

		//analyses user input determines what to do next if the input is less than 60, between 60 and 80 or over 80
		if (weight9<60)
			System.out.println("You are light weight");
		else if (weight9>=60 && weight9<80)
			System.out.println("You are medium weight");
		else 
			System.out.println("You are heavy weight");
		
		//reserves and reads the user's input
		int weight10 = reader.nextInt();

		//analyses user input determines what to do next if the input is less than 60, between 60 and 80 or over 80
		if (weight10<60)
			System.out.println("You are light weight");
		else if (weight10>=60 && weight10 <80)
			System.out.println("You are medium weight");
		else 
			System.out.println("You are heavy weight");
		
	}

}
